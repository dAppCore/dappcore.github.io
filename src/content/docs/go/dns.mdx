---
title: dns
description: DNS primitives — HSD, mainchain, alias, PKI, action.
head:
  - tag: meta
    attrs:
      name: go-import
      content: 'dappco.re/go/dns git https://forge.lthn.sh/core/go-dns.git go'
  - tag: meta
    attrs:
      name: go-source
      content: 'dappco.re/go/dns https://forge.lthn.sh/core/go-dns https://forge.lthn.sh/core/go-dns/tree/dev{/dir} https://forge.lthn.sh/core/go-dns/blob/dev{/dir}/{file}#L{line}'
---

DNS primitives — HSD, mainchain, alias, PKI, action. Built on the [`core/go`](/go/) primitives.

## Install

```sh
go get dappco.re/go/dns@latest
```

## Import

```go
import "dappco.re/go/dns"
```

## Status

Page in flight — content fills in as the package converges. Source of truth
today is the README and inline docstrings in the repo.
